* all functions which return errno_t should return real errno, not just -1.
* it is not necessary to allocate block for the tree node immediately, it might be done in flush time.
* remove aal_block_t from node40 plugin.
