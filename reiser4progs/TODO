* move all libc's replacing functions into separate library libaal. Other functions should be in 
  yet another library.
* all plugins should be libaal independent. In order to reach this we should
  export device working interface at least.
